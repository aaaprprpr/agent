别破坏五个模块的边界，保持解耦，保证初始的默认链路能跑通



提示词
之后提示词改中文，调整各个位置提示词内容，以及插入提示词的时机
渐进式披露提示词，分层次提示词




runtime
尝试一下优化运行流程，提高并行度。
当前react循环是从头到尾循环同一个用户输入，不断往里面添加新的tool输出，很烂。
之后要改成真的react循环，需要模型思考过程，以及工具前后的计划、反思、重试策略。以及失败时候主动请求退出loop




tool
获取时间
读本地文件并解析内容，尽可能支持txt，md，word，ppt
联网搜索，尝试接一下mcp，不然太废物了。
尝试搞个浏览器临时跑代码的沙箱

写文件，写点简单word，md，txt，简单代码文件啥的
给终端权限运行文件



上下文管理
上下文分层次抽象
历史记录的压缩和检索
rag
任务进度记忆，专门用来记忆当前的核心开发项目的进度以及相关对话




llm后端
高并发，高吞吐，降低延迟。